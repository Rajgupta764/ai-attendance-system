# Server Configuration
PORT=5000  # Heroku sets this automatically, but keep for reference
NODE_ENV=production  # Changed from development for production mode

# MongoDB Configuration
MONGODB_URI=mongodb+srv://smartsonuraj76458_db_user:rajkumar@cluster0.lw5nr3t.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0  # Your Atlas URI – good as is

# JWT Secret (Generate a new strong one, e.g., via terminal: openssl rand -hex 32)
JWT_SECRET=your_new_super_secret_jwt_key_here_min_32_chars_long_and_random  # Replace with a generated secret
JWT_EXPIRE=7d

# Cloudinary Configuration (for image uploads) – Keep as is
CLOUDINARY_CLOUD_NAME=dopvjcnfk
CLOUDINARY_API_KEY=711715686393849
CLOUDINARY_API_SECRET=ZrsyVYCngPAKGbiqCcptzaJkBBo

# AI Service Configuration (Python Flask/FastAPI) – Update after AI service deployment
AI_SERVICE_URL=https://your-ai-service-app.herokuapp.com/api/recognize  # Heroku URL for AI service (set after deploy)
AI_SERVICE_ENABLED=true

# CORS Origin – Update after frontend deployment
CORS_ORIGIN=https://your-frontend.vercel.app  # Vercel URL for frontend (set after deploy)

# Admin Default Credentials (First-time setup) – Change password for security if needed
ADMIN_EMAIL=admin@attendance.com
ADMIN_PASSWORD=admin123
ADMIN_NAME=System Admin